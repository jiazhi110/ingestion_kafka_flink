name: Flink ETL CI/CD Pipeline (Build/Test/Push/Trigger Infra)
# 整体名称：描述pipeline的全流程，从CI到触发CD

on:
  push:
    branches:
      - main  # 触发分支：main（prod环境）
      - develop  # develop（dev环境）

jobs:
  build-test-push:
    runs-on: ubuntu-latest  # 运行环境：标准Ubuntu VM，适合Java/Docker构建
    permissions:
      id-token: write  # OIDC权限：允许GitHub向AWS请求临时凭证
      contents: read   # 读权限：拉取repo代码
    outputs:
      image: ${{ steps.build-image.outputs.image }}  # job输出：镜像URI，供下游dispatch使用
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4  # 升级到v4：更快拉取代码，支持子模块

      - name: Set up JDK 11
        uses: actions/setup-java@v4  # v4：优化Maven缓存，JDK11适合Flink
        with:
          java-version: '11'
          distribution: 'temurin'  # Eclipse Temurin：稳定开源JDK
          cache: maven  # 缓存：加速mvn下载依赖

      - name: Run Flink Job Tests
        working-directory: ./flink_jobs  # 工作目录：Flink源代码位置
        run: mvn test  # 运行测试：JUnit等；失败时pipeline中止，确保质量

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4  # OIDC认证：安全，无需Access Key
        with:
          role-to-assume: ${{ secrets.AWS_IAM_ROLE_ARN }}  # IAM角色：需ECR push权限
          aws-region: ${{ secrets.AWS_REGION }}  # 区域：从secrets注入

      - name: Log in to Amazon ECR
        id: login-ecr  # 步骤ID：输出registry URI
        uses: aws-actions/amazon-ecr-login@v2  # ECR登录：临时token

      - name: Build, tag, and push image to Amazon ECR
        id: build-image  # 步骤ID：输出完整镜像URI
        env:
          ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}  # 注册表URI：从登录步骤获取
          # ECR_REPOSITORY: ${{ github.ref_name == 'main' && secrets.PROD_ECR_REPO || secrets.DEV_ECR_REPO }}  # 多环境逻辑：main用prod repo，develop用dev repo
          ECR_REPOSITORY: ${{ secrets.ECR_REPOSITORY_NAME }}  #暂时用这个，回头如果用了在本地cd了一次后，生成了 ECR，然后再用那两个，记得要重新建立secret key
        run: |
          # 构建自定义标签：branch + short SHA，便于追溯版本
          SHORT_SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          IMAGE_TAG="${{ github.ref_name }}-${SHORT_SHA}"
          docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG .  # 构建镜像：假设Dockerfile在root或./flink_jobs
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG  # 推送版本标签
          
          # 推送latest标签：覆盖更新，供Terraform data源拉取最新
          docker tag $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG $ECR_REGISTRY/$ECR_REPOSITORY:latest
          docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest
          
          echo "image=$ECR_REGISTRY/$ECR_REPOSITORY:latest" >> $GITHUB_OUTPUT  # 输出latest URI到job outputs

      # 自己玩暂时不用
      # - name: Send Email Notification
      #   if: always()  # 条件：始终运行（success/failure），用于警报
      #   uses: dawidd6/action-send-mail@v4  # 邮件action：SMTP支持Gmail等
      #   with:
      #     server_address: ${{ secrets.SMTP_HOST }}  # SMTP服务器：e.g., smtp.gmail.com
      #     server_port: 587  # 端口：TLS加密
      #     username: ${{ secrets.SMTP_USER }}  # 用户名：从secrets
      #     password: ${{ secrets.SMTP_PASS }}  # 密码：app password，非主密码
      #     subject: 'Flink ETL CI: ${{ job.status }} on ${{ github.ref_name }}'  # 主题：动态状态+分支
      #     to: ${{ secrets.EMAIL_TO }}  # 收件人：你的邮箱
      #     from: GitHub Actions <noreply@github.com>  # 发件人：固定
      #     body: |  # 邮件正文：多行，包含URI和日志链接
      #       Build ${{ job.status }} for commit ${{ github.sha }}.
      #       Image URI: ${{ steps.build-image.outputs.image }}
      #       Logs: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}

  # trigger-infra-cd:  # 新增job：CI成功后触发infra repo的CD
  #   needs: build-test-push  # 依赖：等待CI完成
  #   runs-on: ubuntu-latest
  #   if: always()  # 条件：始终触发（即使CI失败，便于debug infra）
  #   steps:
  #     - name: Dispatch to data-platform-infra
  #       uses: peter-evans/repository-dispatch@v3  # dispatch action：跨repo事件触发
  #       with:
  #         token: ${{ secrets.GITHUB_TOKEN }}  # token：默认GITHUB_TOKEN，需repo write权限
  #         repository: your-org/data-platform-infra  # 目标repo：替换成你的org/repo（e.g., username/data-platform-infra）
  #         event-type: flink-deploy  # 事件类型：infra workflow监听这个（自定义）
  #         client-payload: >  # payload：JSON数据，传给infra（ref、URI、环境）
  #           {
  #             "ref": "${{ github.ref_name }}",
  #             "image_uri": "${{ needs.build-test-push.outputs.image }}",
  #             "sha": "${{ github.sha }}",
  #             "environment": "${{ github.ref_name == 'main' && 'prod' || 'dev' }}"
  #           }

  #     - name: Send Dispatch Email  # 通知：dispatch结果邮件
  #       if: always()
  #       uses: dawidd6/action-send-mail@v4
  #       with:
  #         # 配置同上（复用SMTP secrets）
  #         subject: 'Flink ETL Trigger Infra: ${{ job.status }}'
  #         body: |
  #           Dispatched to data-platform-infra with image: ${{ needs.build-test-push.outputs.image }}.
  #           Environment: ${{ github.ref_name == 'main' && 'prod' || 'dev' }}.